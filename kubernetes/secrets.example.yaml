apiVersion: v1
kind: Secret
metadata:
  name: senhas
type: Opaque # para o valor não ficar a mostra
stringData:
  SENHA_POSTGRES_AUTENTICACAO: "exemplo"
  SENHA_ASSINATURA_JWT_AUTENTICACAO: "exemplo"
  SENHA_POSTGRES_LOJA: "exemplo"
  DB_CONNECTION_STRING_LOJA: "Host=svc-postgres-loja;Database=loja;Username=exemplo;Password=exemplo"
  RABBITMQ_PASSWORD: "exemplo"